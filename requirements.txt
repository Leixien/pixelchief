# BasePilot runtime dependencies (versions verified working on Windows 11 / Python 3.13).
PySide6>=6.11
opencv-python>=4.12
numpy>=2.2
pytesseract>=0.3.13
pywin32>=306; sys_platform == "win32"

# Tesseract itself is NOT a pip package: place a Tesseract 5 install (tesseract.exe,
# its DLLs and tessdata/) in tesseract_bundle/ — see README.
# Build the exe with: pyinstaller BasePilot.spec   (pip install pyinstaller)
